#include <vector>
#include <list>

unsigned ShellSort(std::vector<int>&);
void swap_shell(std::vector<int> &, unsigned int, unsigned int);
unsigned CountingSort(std::vector<int>&);
unsigned CombSort(std::vector<int>&);